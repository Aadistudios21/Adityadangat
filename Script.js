// script.js

// Function to toggle a CSS class for animation
function toggleAnimation() {
  const animatedSection = document.querySelector('.animated-section');
  animatedSection.classList.toggle('animate');
}

// Add event listener to trigger animation on page load
window.addEventListener('load', toggleAnimation);

// You can add more event listeners or functions for additional interactions
￼Enter
